# MAGE Backend

Backend package metadata lives here so local editable installs and Docker builds
can resolve the project readme during installation.

See the repository root `README.md` for full project documentation.