# ─── API Keys ────────────────────────────────────────────────────────────────
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=...
GEMINI_API_KEY=...

# ─── Backend ─────────────────────────────────────────────────────────────────
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
LOG_LEVEL=INFO
ENVIRONMENT=development

# ─── Frontend ────────────────────────────────────────────────────────────────
NEXT_PUBLIC_API_URL=http://localhost:8000
FRONTEND_PORT=3000

# ─── Vector Store ────────────────────────────────────────────────────────────
# Chroma runs in-process (embedded PersistentClient) — no separate server.
VECTOR_STORE_BACKEND=chroma     # Options: chroma | faiss
FAISS_INDEX_PATH=./data/faiss_index
CHROMA_DB_PATH=./data/chroma_db

# ─── Data Pipeline ───────────────────────────────────────────────────────────
MAX_UPLOAD_SIZE_MB=100
PROCESSING_ENGINE=pandas         # Options: pandas | spark | dask

# ─── Authentication / JWT ────────────────────────────────────────────────────
# Generate a secure key:  python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=CHANGE-ME-IN-PRODUCTION-use-openssl-rand-hex-32
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# ─── Authentication / Google OAuth ───────────────────────────────────────────
# Generate a secure key:  python -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET_KEY=CHANGE-ME-SESSION-SECRET
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
FRONTEND_URL=http://localhost:3000
# This backend's own public URL — Google redirects here after consent, so it
# must exactly match an "Authorized redirect URI" in Google Cloud Console
# (that URI is this value + /auth/google/callback). Wrong in a deployed
# environment and Google OAuth silently redirects to an unreachable localhost.
BACKEND_URL=http://localhost:8000

# ─── Database ────────────────────────────────────────────────────────────────
# For docker compose, the backend container overrides this to point at the
# `postgres` service — see docker-compose.yml. This value is used for running
# the backend natively (outside Docker), pointing at a local Postgres.
DATABASE_URL=postgresql+asyncpg://mage:mage@localhost:5432/mage
POSTGRES_USER=mage
POSTGRES_PASSWORD=mage
POSTGRES_DB=mage
POSTGRES_PORT=5432

# Only needed behind a TLS-intercepting proxy or VPN (Zscaler and friends),
# which makes every OCR call fail with "self-signed certificate in certificate
# chain". Export the proxy's root CA as a PEM file and point this at it.
OCR_CA_BUNDLE=
