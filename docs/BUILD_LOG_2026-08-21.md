# MAGE — Build Log, 21 August 2026

> Companion to `BUILD_LOG_2026-08-18.md` (18 Aug).
> **Branch:** `feat/m2-goal-orchestrator` · **Submission:** 25 Aug 2026 (4 days)

---

## 1. Headline

Merged Bhagyasree's observe-and-replan ReAct loop after verifying it rather than
trusting its commit message, then closed the two weaknesses that verification
found. **No new product behaviour** — the demo output is byte-identical before
and after — but the ReAct loop now has tests that can actually fail.

| | Start of day | End of day |
|---|---|---|
| Tests | 750 | **776** |
| Chartless report | renders nothing at all | **heading + the agent's reason** |
| Orchestrator run length | fixed 4 steps | **4–7, data-dependent** |
| `MAX_REACT_STEPS` | dead code by its own admission | guards a real variable-length loop |
| Vacuous tests known | 0 | 0 (one found and closed) |

---

## 2. The stack was down, and "no charts" is how that looks

Reported symptom: the report page showed no visualisations. Cause: Docker
Desktop was not running — no listeners on 3000/8000 at all.

Worth recording because the symptom is ambiguous by construction. The report
page does `if (specs.length === 0) return null`
(`frontend/app/dashboard/analysis/[id]/page.tsx`), so a chartless run removes
the Visualizations heading entirely, with no empty state. **A dead backend and a
genuinely empty chart set look identical in the browser.** Fixed in §7.

Ruled out a real viz defect by running the pipeline inside the container rather
than trusting the UI: `saas_customers.csv` + "Find natural segments among our
customers" → `k=2, silhouette=0.481`, exactly `cluster_scatter` + `pairplot`.

---

## 3. Merging 6e11c24 — verified, not taken on trust

`integration/combined-features` had moved by one commit: Bhagyasree's
`feat(orchestrator): make the ReAct loop genuinely observe-and-replan`, which
closes the Objective 3 gap. `_reflect_on_mining()` reads three signals Mining
already computes (KMeans silhouette, attribution `model_score`, target
completeness) and can insert 1–3 reflect steps that change what Visualization
and Recommendation do next.

Verified before merging, in a throwaway worktree so the branch stayed clean:

- **Merge is clean.** Rehearsed as a real merge, not just `merge-tree`. Nine
  files, no conflicts — our only unique commit since her fork point was docs.
- **775 passed, 1 skipped** reproduced independently, up from 750, every
  pre-existing test passing unmodified.
- **The tests bind to the behaviour.** Neutering `_reflect_on_mining()` to
  `return []` failed 6 of the 13 new tests.

Merged as `c1e4ba9`, with the verification recorded in the merge message.

### 3.1 What it changes for the demo: nothing

All five walkthrough goals over `data/samples/saas_customers.csv` still run in
exactly 4 steps with zero reflections. Silhouette 0.481 sits above the 0.35 weak
threshold, so the clustering charts and their titles are unchanged.

The loop is real but **invisible on this dataset**. Showing it needs data that
crosses a trigger — a heavily-missing target is the easiest to stage.

---

## 4. The vacuous test the mutation check found

`TestEndToEndAdaptiveRun::test_a_dataset_with_a_near_random_target_produces_a_reflection_step`
**passed with reflection entirely disabled.** Its assertions sat inside
`if "OrchestratorAgent" in agent_names:` — when nothing fires, the branch never
runs and the test goes green. The name promised a reflection step was produced;
nothing asserted one was.

It was a deliberate trade against flake (GradientBoosting can overfit noise at
small n and score above the trust threshold), but the result was that the only
end-to-end test of the adaptive loop could not fail.

**Fixed by changing the trigger, not the assertions.** Target missingness is
arithmetic over a column, so a 50%-null target is below
`LOW_TARGET_COMPLETENESS_PCT` by construction — deterministic in a way that
judging a model's fit on noise is not. The test now asserts unconditionally that
a reflect step exists, that it names the real column, and that the decision
reaches `RecommendationAgent` rather than only being logged.

Re-ran the mutation: **6 failures → 7.** The new test is now one of them.

This is the third vacuous-test near-miss on this project. The pattern each time
is the same: an assertion that can be skipped rather than one that can be wrong.

---

## 5. The substring coupling between two agents

`RecommendationAgent` dropped the untrusted attribution pattern by matching the
literal `"' contributes most to predicting"`, against a sentence built 300 lines
away in `MiningAgent._build_patterns`. Two copies of one string, in two files,
with nothing holding them together.

The failure mode is quiet: change the wording in Mining, and the filter stops
filtering while every existing test stays green — because all three tests of
that filter **hand-write the pattern as a literal**, so they test the filter
against a string Mining no longer produces.

Confirmed it rather than assuming: drifting Mining's wording to "is the dominant
predictor of" left all 22 recommendation tests passing while an untrusted
attribution reached the reader as a top finding.

**Fix, in two parts.**

`ATTRIBUTION_PATTERN_MARKER` now lives in `mining_agent.py` beside the only code
that produces it, and `recommendation_agent.py` imports it. Producer output is
byte-identical.

And a test that binds the two — `test_the_filter_matches_a_pattern_mining_actually_produced`
asks `MiningAgent` to build the pattern instead of hand-writing it. The first
version of this test was itself vacuous: asserting the old phrase was *absent*
passes trivially once the phrase changes. It now asserts on the **feature name**
carried by the pattern, with a trusted-run control proving the name reaches the
reader when the filter is off.

Verified by changing the constant's value alone: the filter test passes (both
sides moved together) while `test_feature_attribution.py`'s literal assertions
fail — which is the intended division. The constant makes the coupling
structural; the test literals pin the user-visible sentence, so changing the
wording fails tests named about phrasing rather than silently altering what
readers see.

---

## 6. State at end of day

- **776 passed, 1 skipped.**
- `feat/m2-goal-orchestrator` pushed; `main` untouched at `b64ed19`.
- Backend image rebuilt against the merge — worth remembering that
  `docker-compose.yml` mounts only `./data`, so source changes need
  `docker compose up -d --build backend` before they exist in the browser.

### Not done

- `LOW_ATTRIBUTION_MODEL_SCORE` is an absolute cutoff, not one relative to a
  majority-class baseline — Bhagyasree documents this in-line as a known
  follow-up, and it still stands.
- The three older filter tests still hand-write the attribution literal. They
  pass, and they now act as extra wording pins, but they are named as filter
  tests and would fail for a phrasing reason.

---

## 7. The chartless report now says so

The `return null` from §2. An empty spec list removed the Visualizations
section from the page entirely, so a run that produced no chart was
pixel-identical to a backend that never answered.

**Confirmed the state is reachable before building UI for it**, rather than
assuming. A single-column CSV still trips the known delimiter bug (F3 in the
remaining plan), ingestion raises, and the run continues to a Visualization step
that returns `status: success` with zero specs and the message *"No mining
output or dataset available — visualization skipped."* So today's most likely
chartless report is one where something upstream already went wrong — precisely
the case where showing the reader nothing is worst.

The section now renders a heading and **the agent's own message** rather than
wording invented in the component, which would drift from what actually
happened. Three cases: no Visualization step at all, a step that did not
succeed (shows its observation), and a successful step with no specs (shows its
message). The reason sits on its own line — the agent's messages are full
sentences that often carry their own em-dash clause, and joining one inline
after "No charts for this run —" reads as a run-on.

No frontend test infrastructure exists (`package.json` has only `lint` and
`build`), so this is verified by typecheck, lint, a clean production build, and
confirming the string ships in both the SSR chunk and the client bundle. **It
has not been rendered in a browser** — that needs an account, an upload of a
file that trips the delimiter bug, and the report page open.
